<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.cto.auction.repository.testRep">
	<resultMap id="testResultMapId" type="testAlias" />
	<select id="testSrvcMethod" resultMap="testResultMapId"
		parameterType="testAlias">
		select * from test
		<!-- where 1=1 <if test="no!=null">and DATANO like '%'||#{DATANO}||'%'</if> -->
	</select>
</mapper>
